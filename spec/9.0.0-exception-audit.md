# Exception Audit & Specialization

**Status:** Implemented
**Branch:** `feature/exceptions-15g`
**Scope:** Replace generic PHP exceptions with domain-specific custom exceptions across `src/`, `tests/`, and `docs/`. Rename misleading exception classes. Fix typos.

---

## Motivation

The codebase has 20 well-designed custom exceptions in `src/Exceptions/`, but `src/` also throws **~70 generic PHP exceptions** (`InvalidArgumentException`, `RuntimeException`) with inline message strings. These:

1. Cannot be caught selectively — callers who want to handle "invalid machine config" must catch all `InvalidArgumentException`s
2. Scatter message construction across throw sites instead of centralizing in factory methods
3. Break the established pattern where every domain error has a named exception with `::build()` / named constructors
4. Make it impossible to write targeted `catch` blocks or test `expectException(SpecificClass::class)`

Since v9 is unreleased, we can make these BC-breaking changes freely.

---

## Scope Rules

### What's in scope

Every generic exception thrown from **domain code** (`src/` excluding `src/Testing/`) must be replaced with a custom exception class. When the exception class changes in `src/`, **all three layers** update in the same commit:

1. **`src/`** — Create/use new exception class, replace throw sites
2. **`tests/`** — Update all assertions (`->toThrow()`, `->throws()`, `catch` blocks, `use` imports) that verify the changed exception
3. **`docs/`** — Update all markdown files that reference the old exception class name

### What's out of scope

| Category | Location | Why | Examples |
|---|---|---|---|
| Test stubs simulating external failures | `tests/Stubs/`, inline closures | These represent arbitrary external errors, not EventMachine domain exceptions | `throw new RuntimeException('Payment gateway down')`, `$job->failed(new RuntimeException('API timeout'))` |
| Testing infrastructure guard clauses | `src/Testing/` (TestMachine, InlineBehaviorFake, HasBuilder) | Developer-facing test API errors, not machine runtime errors | TestMachine L169 "State not found", InlineBehaviorFake L205 "class-based behavior" |
| PHPUnit assertion exceptions | `src/Testing/`, `src/Actor/Machine.php` | PHPUnit convention — `AssertionFailedError` is the standard | ~35 `AssertionFailedError` sites in TestMachine/Machine |
| Deprecation warnings | 4 sites across src/ | `trigger_error` with `E_USER_DEPRECATED`, not exceptions | Colon syntax deprecation warnings |
| `@throws` docblock cleanup | Across src/ | Follow-up task, not this spec | |
| Catch block broadness | Across src/ | Separate effort — some catch `\Throwable` broadly | |

**Symmetry rule:** If a `src/` throw site stays unchanged, the test assertions verifying it also stay unchanged (see Groups L, M, N for explicit lists).

### Completion check

After all groups, run:

```bash
# Domain code: zero generic exceptions (excluding src/Testing/)
grep -rn "throw new \\\\InvalidArgumentException\|throw new \\\\RuntimeException\|throw new InvalidArgumentException\|throw new RuntimeException" src/ --include="*.php" | grep -v "src/Testing/"

# Deleted/renamed classes: zero hits anywhere
grep -rn "InvalidFinalStateDefinitionException\|NoStateDefinitionFoundException" src/ tests/ docs/ --include="*.php" --include="*.md"
```

---

## Groups

### Group A — StateConfigValidator (42 instances → 1 new class)

**Files:** `src/StateConfigValidator.php`, `src/Definition/StateDefinition.php`, `src/Definition/MachineDefinition.php`
**New Exception:** `InvalidStateConfigException extends LogicException`

Also absorbs `InvalidFinalStateDefinitionException` (delete after merge — 0 test references). Its throw sites in `StateDefinition.php:579` (`noChildStates`) and `MachineDefinition.php:935` (`noTransitions`) also migrate to `InvalidStateConfigException::finalStateCannotHaveChildStates()` / `finalStateCannotHaveTransitions()`. Keeps `InvalidParallelStateDefinitionException` separate (has runtime checks in `MachineDefinition.php`).

**Factory methods:**

| Factory Method | Line | Message Pattern |
|---|---|---|
| `invalidRootKeys(array $keys)` | L91 | Invalid root level keys: {keys} |
| `invalidListenConfig()` | L105 | 'listen' must be an array |
| `invalidListenKeys(array $keys)` | L113 | Invalid 'listen' keys: {keys} |
| `statesMustBeArray()` | L60 | States configuration must be an array |
| `alwaysMustBeUnderOn(string $path)` | L160 | @always must be under 'on' |
| `invalidStateKeys(string $path, array $keys)` | L173 | Invalid keys in state '{path}' |
| `invalidStateType(string $path, string $type)` | L272 | Invalid type in state '{path}' |
| `finalStateCannotHaveTransitions(string $path)` | L287 | Final state cannot have transitions |
| `finalStateCannotHaveChildStates(string $path)` | L293 | Final state cannot have child states |
| `parallelStateMustHaveRegions(string $path)` | L311 | Parallel state must have regions |
| `invalidStatesConfig(string $path)` | L241 | States must be an array |
| `emptyDoneDotSuffix(string $path)` | L211 | @done. suffix cannot be empty |
| `doneDotRequiresMachine(string $path)` | L222 | @done.{state} requires machine |
| `invalidTransitionsConfig(string $path)` | L354 | 'on' must be an array |
| `invalidTransitionFormat(string $path, string $event)` | L397 | Transition must be string or array |
| `invalidTransitionKeys(string $path, string $event, array $keys)` | L432 | Invalid keys in transition |
| `invalidBehaviorConfig(string $path, string $event, string $behavior)` | L462 | Guards/actions/calculators must be array or string |
| `emptyGuardedTransitions(string $path, string $event)` | L477 | Empty conditions array |
| `invalidGuardedCondition(string $path, string $event)` | L485 | Each condition must be an array |
| `defaultGuardMustBeLast(string $path, string $event)` | L493 | Default condition must be last |
| `invalidDoneFailConfig(string $path, string $key)` | L516 | Must be string or array |
| `invalidMachineValue(string $path)` | L542 | Must be a string FQCN |
| `machineAndParallelConflict(string $path)` | L549 | Cannot have both |
| `forwardRequiresQueue(string $path)` | L556 | Forward requires async mode |
| `invalidForwardKeys(string $path, string $entry, array $keys)` | L570 | Unknown forward keys |
| `forwardUriMustStartWithSlash(string $path, string $entry)` | L577 | URI must start with / |
| `fireAndForgetCannotHaveFail(string $path)` | L592 | No @fail without @done |
| `fireAndForgetCannotHaveTimeout(string $path)` | L601 | No @timeout without @done |
| `fireAndForgetCannotHaveOutput(string $path)` | L610 | No output without @done |
| `fireAndForgetCannotHaveForward(string $path)` | L620 | No forward without @done |
| `targetRequiresQueue(string $path)` | L631 | Target requires async |
| `targetAndDoneConflict(string $path)` | L639 | Cannot have both |
| `uncoveredChildFinalStates(string $path, string $machineClass, array $states)` | L783 | Uncovered final states |
| `crossRegionTransition(string $source, string $target, string $region)` | L836 | Cross-region not allowed |
| `invalidJobValue(string $path)` | L661 | Must be string FQCN |
| `jobAndMachineConflict(string $path)` | L668 | Cannot have both |
| `jobAndParallelConflict(string $path)` | L675 | Cannot have both |
| `forwardWithJobNotAllowed(string $path)` | L682 | Forward only for machines |
| `jobRequiresDoneOrTarget(string $path)` | L689 | Must have @done or target |
| `jobDoneAndTargetConflict(string $path)` | L696 | Cannot have both |
| `valueMustBeStringArrayOrNull()` | L721 | Normalization error |

**Tests:** `InvalidArgumentException::class` → `InvalidStateConfigException::class`

| Test File | Lines |
|---|---|
| `tests/Integration/StateConfigValidatorTest.php` | ~60 refs (import + all assertions) |
| `tests/Features/ListenValidatorTest.php` | L44, L57 |
| `tests/Features/TransitionTieBreakerTest.php` | L38, L66 |
| `tests/Features/FireAndForgetMachineDelegationTest.php` | L108–L191, L620 |
| `tests/Features/JobActorTest.php` | L33, L49, L66 only (NOT L271/L286/L296/L308 — those are Group F) |
| `tests/Features/MachineDelegationTest.php` | L564, L581, L599 |
| `tests/Integration/CrossRegionTransitionValidationTest.php` | L59, L99, L139, L180 |
| `tests/Features/ForwardEndpointValidationTest.php` | L44, L73, L101, L128 only (L166+ is Group C) |
| `tests/Features/ParallelStates/BasicParallelStatesTest.php` | L244 |
| `tests/Features/ParallelStates/ParallelDispatchConfigTest.php` | L88 |

**Docs:** `InvalidFinalStateDefinitionException` → `InvalidStateConfigException`

| Doc File | Line |
|---|---|
| `docs/building/defining-states.md` | L93 |

---

### Group B — MachineRouter (8 instances → 1 new class)

**File:** `src/Routing/MachineRouter.php`
**New Exception:** `InvalidRouterConfigException extends LogicException`

| Factory Method | Line | Message Pattern |
|---|---|---|
| `onlyAndExceptConflict()` | L58 | Cannot use both only and except |
| `unknownEventTypes(string $filterKey, array $unknown, array $available)` | L81 | Unknown event types in filter |
| `forwardedInMachineIdFor(array $events)` | L128 | Forwarded endpoints cannot be in machineIdFor |
| `forwardedInModelFor(array $events)` | L140 | Forwarded endpoints cannot be in modelFor |
| `orphanedMachineIdFor(array $orphans, ?string $filterContext)` | L164 | machineIdFor references unregistered events |
| `orphanedModelFor(array $orphans, ?string $filterContext)` | L184 | modelFor references unregistered events |
| `modelAndAttributeRequired()` | L194 | model and attribute required when modelFor is set |
| `overlappingBindingModes(array $overlap)` | L202 | Events in both machineIdFor and modelFor |

**Tests:** `InvalidArgumentException::class` → `InvalidRouterConfigException::class`

| Test File | Lines |
|---|---|
| `tests/Routing/MachineRouterTest.php` | L424–L724 (~20 refs) |

**Docs:**

| Doc File | Line |
|---|---|
| `docs/laravel-integration/endpoints.md` | L709 |

---

### Group C — MachineDefinition forward conflicts (3 instances → extend existing)

**File:** `src/Definition/MachineDefinition.php`
**Target:** Add factory methods to existing `InvalidEndpointDefinitionException`

| Factory Method | Line | Message Pattern |
|---|---|---|
| `forwardConflictsWithEndpoint(string $state, string $event)` | L281 | Forward event clashes with parent endpoint |
| `forwardConflictsWithBehaviorEvent(string $state, string $event)` | L289 | Forward event clashes with behavior event |
| `duplicateForwardEvent(string $event)` | L297 | Same forward event in multiple states |

**Tests:** `InvalidArgumentException::class` → `InvalidEndpointDefinitionException::class`

| Test File | Lines |
|---|---|
| `tests/Features/ForwardEndpointParsingTest.php` | L426, L465, L508, L541 |
| `tests/Features/ForwardEndpointEdgeCasesTest.php` | L140, L175–L176 (update `catch` block) |
| `tests/Features/ForwardEndpointValidationTest.php` | L166, L194, L231, L265, L304–L305, L339–L340 (update `catch` blocks) |

**Docs:** None.

---

### Group D — InvokableBehavior parent communication (2 instances → 1 new class)

**File:** `src/Behavior/InvokableBehavior.php`
**New Exception:** `NoParentMachineException extends RuntimeException`

| Factory Method | Line | Message Pattern |
|---|---|---|
| `cannotSendToParent()` | L133 | Machine was not invoked by a parent (sendToParent) |
| `cannotDispatchToParent()` | L160 | Machine was not invoked by a parent (dispatchToParent) |

**Tests:** `RuntimeException::class` → `NoParentMachineException::class`

| Test File | Lines |
|---|---|
| `tests/Features/SendToTest.php` | L155 (`'Cannot sendToParent'`), L231 (`'Cannot dispatchToParent'`) |

**Docs:**

| Doc File | Line |
|---|---|
| `docs/advanced/sendto.md` | L95 |

---

### Group E — Archive & Compression (6 instances → 1 new class)

**Files:** `src/Models/MachineEventArchive.php`, `src/Support/CompressionManager.php`
**New Exception:** `ArchiveException extends RuntimeException`

| Factory Method | File | Line | Message Pattern |
|---|---|---|---|
| `emptyEventCollection()` | MachineEventArchive | L80 | Cannot archive empty collection |
| `compressionFailed()` | MachineEventArchive | L98 | gzcompress returned false |
| `decompressionFailed()` | MachineEventArchive | L125 | gzuncompress returned false |
| `invalidCompressionLevel(int $level)` | CompressionManager | L50 | Level must be 0-9 |
| `decompressDataFailed()` | CompressionManager | L152 | Generic decompression failure |
| `invalidData()` | CompressionManager | L162 | Neither compressed nor valid JSON |

**Tests:**

| Test File | Lines | Old → New |
|---|---|---|
| `tests/Support/CompressionManagerTest.php` | L87, L92 | `InvalidArgumentException` → `ArchiveException` |
| `tests/Models/MachineEventArchiveTest.php` | L157 | `InvalidArgumentException` → `ArchiveException` |
| `tests/Integration/ArchiveEdgeCasesTest.php` | L259 | `InvalidArgumentException` → `ArchiveException` |

**Docs:** None.

---

### Group F — Job class validation (4 instances → 2 new classes)

**Files:** `src/Jobs/ChildMachineJob.php`, `src/Jobs/SendToMachineJob.php`, `src/Jobs/ChildJobJob.php`

**New Exception 1:** `InvalidMachineClassException extends LogicException`

| Factory Method | File | Line |
|---|---|---|
| `mustExtendMachine(string $class)` | ChildMachineJob | L64 |
| `mustExtendMachine(string $class)` | SendToMachineJob | L48 |

**New Exception 2:** `InvalidJobClassException extends LogicException`

| Factory Method | File | Line |
|---|---|---|
| `classNotFound(string $class)` | ChildJobJob | L57 |
| `missingHandleMethod(string $class)` | ChildJobJob | L61 |

**Tests:**

| Test File | Lines | Old → New |
|---|---|---|
| `tests/Features/JobActorTest.php` | L271 (`'does not exist'`) | `InvalidArgumentException` → `InvalidJobClassException` |
| `tests/Features/JobActorTest.php` | L286 (`'must have a handle()'`) | `InvalidArgumentException` → `InvalidJobClassException` |
| `tests/Features/JobActorTest.php` | L296, L308 (`'must exist and extend'`) | `InvalidArgumentException` → `InvalidMachineClassException` |

**Docs:** None.

---

### Group G — MachineController output lookup (1 instance → use existing)

**File:** `src/Routing/MachineController.php:266`
**Fix:** `\RuntimeException` → existing `BehaviorNotFoundException::build($outputKey)`

**Tests:** None. **Docs:** None.

---

### Group H — MachineScheduler (2 instances → use existing + extend existing)

**File:** `src/Scheduling/MachineScheduler.php`

| Line | Old | New |
|---|---|---|
| L41 | `\InvalidArgumentException("Event not defined in schedules")` | Existing `InvalidScheduleDefinitionException::undefinedEvent()` |
| L37 | `\InvalidArgumentException("Failed to load definition")` | New `MachineDefinitionNotFoundException::failedToLoad(string $class, \Throwable $previous)` |

**Tests:**

| Test File | Lines | Old → New |
|---|---|---|
| `tests/Features/MachineSchedulerTest.php` | L43 (`'TYPO_EVENT'`) | `InvalidArgumentException` → `InvalidScheduleDefinitionException` |
| `tests/Features/MachineSchedulerTest.php` | L47 (`'INC'`) | `InvalidArgumentException` → `MachineDefinitionNotFoundException` |

**Docs:** None.

---

### Group I — Fakeable trait (4 instances → 1 new class)

**File:** `src/Traits/Fakeable.php`
**New Exception:** `BehaviorNotFakedException extends RuntimeException`

| Factory Method | Lines |
|---|---|
| `build(string $behaviorClass)` | L124, L136, L148, L162 |

**Tests:** `RuntimeException::class` → `BehaviorNotFakedException::class`

| Test File | Lines |
|---|---|
| `tests/Features/Testability/FakeableTraitTest.php` | L152, L167, L185, L205 |

**Docs:**

| Doc File | Line |
|---|---|
| `docs/testing/fakeable-behaviors.md` | L86 (`RuntimeException` → `BehaviorNotFakedException`) |

---

### Group J — MachineConfigValidatorCommand (1 instance → 1 new class)

**File:** `src/Commands/MachineConfigValidatorCommand.php:119`
**New Exception:** `MachineDiscoveryException extends RuntimeException`

| Factory Method | Line |
|---|---|
| `noSearchPaths()` | L119 |

**Tests:** None. **Docs:** None.

---

### Group K — Timer validation (1 instance → 1 new class)

**File:** `src/Support/Timer.php:19`
**New Exception:** `InvalidTimerDefinitionException extends LogicException`

| Factory Method | Line |
|---|---|
| `nonPositiveDuration(int $seconds)` | L19 |

**Tests:** `InvalidArgumentException::class` → `InvalidTimerDefinitionException::class`

| Test File | Lines |
|---|---|
| `tests/Features/TimerVerificationTest.php` | L95, L99 |
| `tests/Features/TimerTest.php` | L45, L49 |
| `tests/Features/TimerConfigValidationTest.php` | L168, L172 |

**Docs:**

| Doc File | Line |
|---|---|
| `docs/advanced/time-based-events.md` | L282 |

---

### Groups L, M, N — Testing infrastructure (keep as-is)

These throw generic exceptions from `src/Testing/` — testing infrastructure, not domain code. Both the throw sites AND their test assertions stay unchanged.

| Group | File | Exception | Test Assertions (unchanged) |
|---|---|---|---|
| L | `src/Testing/TestMachine.php` L169, L391, L2048–L2059 | `\InvalidArgumentException`, `\RuntimeException` | `InlineBehaviorIntegrationTest.php` L87, `TestMachineV2Test.php` L1157 |
| L | `src/Testing/TestMachine.php`, `src/Actor/Machine.php` | `AssertionFailedError` (~35 sites) | PHPUnit convention — no changes |
| M | `src/Testing/InlineBehaviorFake.php` L205 | `\InvalidArgumentException` | `InlineBehaviorFakeTest.php` L75–L82 |
| N | `src/Testing/HasBuilder.php` L45 | `\RuntimeException` | `EventBuilderTest.php` L52 |

---

## Rename: `NoStateDefinitionFoundException` → `UndefinedTargetStateException`

**Reason:** Current name suggests "state definition not found" but it actually means "transition references a target state that doesn't exist". Docblock even says "NoTransitionDefinitionFoundException" — the name is confusing.

**src/:**

| File | What Changes |
|---|---|
| `src/Exceptions/NoStateDefinitionFoundException.php` | Rename file + class |
| `src/Definition/TransitionBranch.php` | `use` import + throw site |
| `src/Definition/MachineDefinition.php` | `use` import + `catch` blocks (L2973+) |
| `src/Testing/TestMachine.php` | `use` import + `catch` blocks (L619, L646, L953) |
| `src/Jobs/SendToMachineJob.php` | `use` import + `catch` block (L68) |
| `src/Actor/Machine.php` | `@throws` docblock (L541) |

**Tests:**

| Test File | Lines |
|---|---|
| `tests/Definition/TransitionDefinitionTest.php` | L11, L124, L138, L142, L154 |

Note: ~25 test files reference `NoTransitionDefinitionFoundException` — that's the *other* exception, no change needed.

**Docs:** Grep at implementation time — none found in current scan.

---

## Bug Fix

| File | Line | Issue | Fix |
|---|---|---|---|
| `MachineAlreadyRunningException` | L16 | Typo: `{$machineId}`is` (missing space) | `{$machineId}` is` |

---

## Documentation

Exception dokumantasyonu iki katmanda yapilir: **in-context** (ilgili feature sayfasinda) ve **central reference** (tek referans sayfasi).

### In-Context Documentation

Her exception, olusabildigi feature'in docs sayfasinda dokumante edilir. Mevcut docs pattern'lari takip edilir:
- Basit durumlar: inline prose mention (`` `ExceptionName` is thrown when... ``)
- Orta durumlar: try/catch code block (`no_run` tag ile)
- Detayli durumlar: dedicated subsection (## Exception Handling veya ## Error Cases)

Asagidaki tablo **tum** exception'lari kapsar: yeni olusturulanlar, mevcut olup zaten dokumante edilenler, ve mevcut olup henuz dokumante edilmeyenler.

#### Feature docs eksikleri — exception'dan once konu anlatilmali

Bazi exception'larin in-context dokumantasyonu icin once ilgili feature'in kendisi yeterince dokumante edilmeli. Asagidaki tabloda mevcut docs'un durumu ve exception eklemeden once yazilmasi gereken icerik listelenmistir.

| Doc File | Mevcut Durum | Eksik Konu | Yapilacak |
|---|---|---|---|
| `docs/building/defining-states.md` | State tanimlama iyi anlatilmis | Config validation kavrami yok — StateConfigValidator nedir, nasil calisir, `artisan machine:validate-config` ne yapar | Yeni section: "## Configuration Validation" — validator'un ne zaman calistigini (define time), hangi kurallari kontrol ettigini, artisan komutuyla nasil kullanildigini anlat |
| `docs/building/writing-transitions.md` | Transition syntax iyi | Transition validation hatalari yok — hangi config'lar gecersizdir, ne olur | Mevcut transition syntax bolumleri icine validation constraint'lerini ekle (ornek: "guards must be array or string", "default guard must be last") |
| `docs/advanced/machine-delegation.md` | Delegation config iyi | Machine class validation yok, fire-and-forget constraint'leri anlatilmis ama validation error olarak degil | Constraint'lerin yanina "throws `InvalidStateConfigException`" ve "`InvalidMachineClassException`" ekle |
| `docs/laravel-integration/endpoints.md` | Endpoint definition + router options tabloda var | Router option validation yok — only/except conflict, orphaned refs, overlapping modes ne zaman olur | Mevcut "Router Options" section'a validation rules ekle veya ayri "## Router Validation" section |
| `docs/testing/fakeable-behaviors.md` | Faking iyi anlatilmis | Unfaked behavior assertion hatalari eksik | Mevcut `fake()` vs `spy()` tablosuna "Asserting without faking" satirini ekle, `BehaviorNotFakedException` anlat |
| `docs/advanced/time-based-events.md` | Timer syntax iyi | Timer duration validation yok — hangi degerler gecerli, 0/negatif ne olur | `after`/`every` syntax bolumine "Duration must be positive" constraint'i ekle |
| `docs/advanced/scheduled-events.md` | Schedule definition iyi | Schedule config validation yok — undefined event type ne olur | "Edge Cases" section'a undefined event validation ekle |
| `docs/behaviors/outputs.md` | Output yazma iyi | Output placement kurallari eksik — transient state'te ve parallel region'da output tanimlanamaz | Yeni section veya mevcut section'a placement rules ekle |
| `docs/laravel-integration/persistence.md` | Locking mention var | Lock timeout config ve davranisi detaysiz — timeout suresi, immediate mode, holder bilgisi | Mevcut locking bolumine config + timeout davranisi ekle |
| `docs/building/defining-states.md` (listeners section) | Listeners `defining-states.md` icinde anlatilmis, ayri dosya yok | Listener config validation eksik — eski format reject davranisi yok | Mevcut listeners bolumine config format validation'i ve `InvalidListenerDefinitionException` ekle |

**Not:** Bu feature docs eksiklikleri, exception in-context dokumantasyonuyla **ayni commit'te** giderilir. Once feature'i anlat, sonra hata durumunu dokumante et.

---

#### Yeni + genisletilen exception'lar — in-context docs eklenmeli

| Exception | Doc File | Style | Content |
|---|---|---|---|
| `InvalidStateConfigException` | `docs/building/defining-states.md` | Dedicated subsection | Config validation hatalari: invalid keys, invalid types, final state constraints. Ornek: yanlis config + exception. `artisan machine:validate-config` ile catch edilebilecegini belirt |
| `InvalidStateConfigException` | `docs/building/writing-transitions.md` | Inline mention | Transition config hatalari: invalid keys, empty guards, default guard ordering |
| `InvalidStateConfigException` | `docs/advanced/machine-delegation.md` | Inline mention | Machine delegation config: machineAndParallelConflict, fireAndForget constraints |
| `InvalidStateConfigException` | `docs/advanced/job-actors.md` | Inline mention | Job actor config: jobRequiresDoneOrTarget, jobAndMachineConflict |
| `InvalidRouterConfigException` | `docs/laravel-integration/endpoints.md` | Table + try/catch | Router config: only/except conflict, orphaned bindings, overlapping modes. Mevcut "Exception Handling" section'a ekle |
| `NoParentMachineException` | `docs/advanced/sendto.md` | Update existing prose | Mevcut `RuntimeException` mention'ini `NoParentMachineException`'a guncelle |
| `ArchiveException` | `docs/laravel-integration/archival.md` | Inline mention | Archive hatalari: empty collection, compression/decompression failure, invalid data |
| `InvalidMachineClassException` | `docs/advanced/machine-delegation.md` | Inline mention | Child machine class validation: must extend Machine |
| `InvalidJobClassException` | `docs/advanced/job-actors.md` | Inline mention | Job class validation: must exist, must have handle() |
| `BehaviorNotFakedException` | `docs/testing/fakeable-behaviors.md` | Update existing table | Mevcut `RuntimeException` / `BadMethodCallException` mention'larini guncelle |
| `InvalidTimerDefinitionException` | `docs/advanced/time-based-events.md` | Inline mention | Timer duration must be positive |
| `UndefinedTargetStateException` | `docs/building/writing-transitions.md` | Inline mention | Transition references undefined target state |
| `MachineDiscoveryException` | — | None | Nadir edge case, artisan command icinde. Sadece central reference'da |

#### Mevcut exception'lar — zaten dokumante, dogrulanmali

Bu exception'lar mevcut docs'ta zaten aciklanmis. Docs enrichment adiminda mevcut icerik dogrulanir, gerekiyorsa iyilestirilir, ama sifirdan yazmaya gerek yok.

| Exception | Existing Doc File(s) | Status |
|---|---|---|
| `MachineValidationException` | `docs/behaviors/validation-guards.md` L34, `docs/laravel-integration/endpoints.md` L787 | Iyi dokumante — try/catch + HTTP mapping |
| `MachineEventValidationException` | `docs/behaviors/events.md` L138, L518, `docs/building/handling-events.md` L116 | Iyi dokumante |
| `MachineContextValidationException` | `docs/building/working-with-context.md` L198, `docs/advanced/custom-context.md` L384 | Iyi dokumante |
| `MaxTransitionDepthExceededException` | `docs/advanced/always-transitions.md` L430+ | Iyi dokumante — dedicated subsection |
| `NoTransitionDefinitionFoundException` | `docs/understanding/events.md` L65, `docs/building/writing-transitions.md` L328, `docs/reference/execution-model.md` L17 | Iyi dokumante — multi-page |
| `MachineAlreadyRunningException` | `docs/understanding/machine-lifecycle.md` L303, `docs/laravel-integration/endpoints.md` L788, `docs/testing/persistence-testing.md` L409 | Iyi dokumante |
| `MissingBehaviorParameterException` | `docs/behaviors/introduction.md` L161 | Dokumante |
| `MissingMachineContextException` | `docs/behaviors/introduction.md` L223 | Dokumante |
| `InvalidOutputDefinitionException` | `docs/getting-started/upgrading.md` L198 | Sadece upgrading guide'da — feature docs'a da eklenmeli (asagidaki "henuz dokumante edilmemis" tablosunda) |
| `InvalidParallelStateDefinitionException` | `docs/getting-started/upgrading.md` L1145 | Sadece upgrading guide'da — feature docs'a da eklenmeli (asagidaki "henuz dokumante edilmemis" tablosunda) |
| `RestoringStateException` | `docs/testing/localqa.md` L198 | LocalQA context'inde — yeterli |

#### Mevcut exception'lar — henuz dokumante edilmemis

Bu exception'lar hicbir docs sayfasinda gecmiyor. In-context docs eklenmeli.

| Exception | Doc File | Style | Content |
|---|---|---|---|
| `BehaviorNotFoundException` | `docs/behaviors/introduction.md` | Inline mention | Inline key veya class resolve edilemezse atilir. Typo kontrolu onerisi |
| `InvalidBehaviorDefinitionException` | `docs/behaviors/introduction.md` | Inline mention | Behavior tuple format hatalari: empty tuple, missing class, closure in tuple |
| `InvalidListenerDefinitionException` | `docs/building/defining-states.md` (listeners section) | Inline mention | Eski class-as-key format reject edilir, yeni format goster |
| `InvalidScheduleDefinitionException` | `docs/advanced/scheduled-events.md` | Inline mention | Schedule undefined event type reference eder |
| `MachineDefinitionNotFoundException` | `docs/building/defining-states.md` | Inline mention | Machine subclass'ta `definition()` method eksik |
| `MachineLockTimeoutException` | `docs/laravel-integration/persistence.md` | Inline mention | Parallel dispatch'te lock acquisition timeout, caught by Machine → MachineAlreadyRunningException |
| `InvalidOutputDefinitionException` | `docs/behaviors/outputs.md` | Inline mention | Transient state veya parallel region'da output tanimlanamaz (upgrading.md'ye ek olarak feature docs'a da) |
| `InvalidParallelStateDefinitionException` | `docs/advanced/parallel-states/index.md` | Inline mention | Parallel state constraints: regions required, no initial, persistence, Machine subclass (upgrading.md'ye ek olarak feature docs'a da) |
| `InvalidEndpointDefinitionException` | `docs/laravel-integration/endpoints.md` | Inline mention | Tum factory method'lar: undefinedEvent, undefinedOutput, invalidAction (mevcut) + forwardConflictsWithEndpoint, forwardConflictsWithBehaviorEvent, duplicateForwardEvent (yeni — Group C) |

### Upgrading Guide: `docs/getting-started/upgrading.md`

"From 8.x to 9.0" section'a yeni bir alt bolum eklenir. Mevcut v9 degisikliklerin altina (Named Parameters'dan sonra), Migration Checklist'in ustune.

**Eklenecek icerik:**

```markdown
### Exception Specialization (breaking)

v9 replaces generic PHP exceptions (`InvalidArgumentException`, `RuntimeException`) with domain-specific exception classes across the entire codebase. This enables targeted `catch` blocks and clearer error handling.

#### New Exception Classes

| Before (v8) | After (v9) | Thrown From |
|-------------|------------|-------------|
| `InvalidArgumentException` (config validation) | `InvalidStateConfigException` | `StateConfigValidator` |
| `InvalidArgumentException` (router config) | `InvalidRouterConfigException` | `MachineRouter` |
| `RuntimeException` (no parent machine) | `NoParentMachineException` | `InvokableBehavior` |
| `InvalidArgumentException` / `RuntimeException` (archive) | `ArchiveException` | `MachineEventArchive`, `CompressionManager` |
| `InvalidArgumentException` (machine class) | `InvalidMachineClassException` | `ChildMachineJob`, `SendToMachineJob` |
| `InvalidArgumentException` (job class) | `InvalidJobClassException` | `ChildJobJob` |
| `RuntimeException` (behavior not faked) | `BehaviorNotFakedException` | `Fakeable` trait |
| `RuntimeException` (no search paths) | `MachineDiscoveryException` | `MachineConfigValidatorCommand` |
| `InvalidArgumentException` (timer) | `InvalidTimerDefinitionException` | `Timer` |

#### Renamed Exception Classes

| Before (v8) | After (v9) |
|-------------|------------|
| `NoStateDefinitionFoundException` | `UndefinedTargetStateException` |

#### Deleted Exception Classes

| Before (v8) | After (v9) |
|-------------|------------|
| `InvalidFinalStateDefinitionException` | Merged into `InvalidStateConfigException` (`finalStateCannotHaveTransitions()`, `finalStateCannotHaveChildStates()`) |

#### Extended Exception Classes

| Class | New Factory Methods |
|-------|---------------------|
| `InvalidEndpointDefinitionException` | `forwardConflictsWithEndpoint()`, `forwardConflictsWithBehaviorEvent()`, `duplicateForwardEvent()` |
| `MachineDefinitionNotFoundException` | `failedToLoad()` |

#### Migration Steps

If you catch any of the old generic exceptions for EventMachine errors, update your catch blocks:

<!-- doctest-attr: ignore -->
```php
// BEFORE (v8)
use InvalidArgumentException;

try {
    StateConfigValidator::validate($config);
} catch (InvalidArgumentException $e) {
    // caught ALL InvalidArgumentExceptions, not just config errors
}

// AFTER (v9)
use Tarfinlabs\EventMachine\Exceptions\InvalidStateConfigException;

try {
    StateConfigValidator::validate($config);
} catch (InvalidStateConfigException $e) {
    // catches only config validation errors
}
```

<!-- doctest-attr: ignore -->
```php
// BEFORE (v8)
use RuntimeException;

try {
    $context->sendToParent('CHILD_DONE');
} catch (RuntimeException $e) {
    // caught ALL RuntimeExceptions
}

// AFTER (v9)
use Tarfinlabs\EventMachine\Exceptions\NoParentMachineException;

try {
    $context->sendToParent('CHILD_DONE');
} catch (NoParentMachineException $e) {
    // catches only the "no parent" case
}
```
```

Migration Checklist'e de su satirlar eklenir:

```markdown
12. Update `catch (InvalidArgumentException)` blocks that handle EventMachine config errors → specific exception classes (see table above)
13. Update `catch (RuntimeException)` blocks that handle EventMachine runtime errors → specific exception classes
14. Rename `NoStateDefinitionFoundException` → `UndefinedTargetStateException` in any catch blocks or type hints
15. Remove `InvalidFinalStateDefinitionException` imports — now `InvalidStateConfigException`
```

---

### Central Reference Page: `docs/reference/exceptions.md`

Sidebar'da `reference/` altina eklenir (mevcut `execution-model.md` yanina). Tum exception'lari kategorize ederek listeler.

**Sayfa yapisi:**

```markdown
# Exceptions Reference

EventMachine throws specific exceptions for different error categories.
Each exception extends either `LogicException` (developer errors caught at
definition time) or `RuntimeException` (errors that occur during execution).

## Configuration Exceptions

Thrown at definition time when machine config is invalid. These are
`LogicException` subclasses — they indicate a bug in your machine definition,
not a runtime condition.

### InvalidStateConfigException

Thrown by `StateConfigValidator` when machine configuration has structural
errors: invalid keys, wrong state types, conflicting options (machine +
parallel), malformed transitions, cross-region transitions, etc.

- **Extends:** `LogicException`
- **Thrown from:** `StateConfigValidator`, `StateDefinition`, `MachineDefinition`
- **Common causes:** Typos in config keys, final states with transitions, parallel states without regions
- **See:** [Defining States](/building/defining-states), [Writing Transitions](/building/writing-transitions)

### InvalidRouterConfigException

Thrown by `MachineRouter::register()` when endpoint routing options are
inconsistent.

- **Extends:** `LogicException`
- **Thrown from:** `MachineRouter`
- **Common causes:** `only` + `except` together, orphaned `machineIdFor`/`modelFor` refs
- **See:** [Endpoints](/laravel-integration/endpoints)

### InvalidEndpointDefinitionException

Thrown when an endpoint definition references undefined events, outputs,
or invalid actions. Also thrown for forward endpoint conflicts.

- **Extends:** `RuntimeException`
- **Thrown from:** `MachineDefinition`
- **Common causes:** Typo in event type, missing output behavior, forward event collision
- **See:** [Endpoints](/laravel-integration/endpoints)

### InvalidParallelStateDefinitionException

Thrown when parallel state config violates constraints.

- **Extends:** `LogicException`
- **Thrown from:** `MachineDefinition`, `StateConfigValidator`
- **Common causes:** Parallel state without regions, with `initial`, without persistence, without Machine subclass
- **See:** [Parallel States](/advanced/parallel-states/)

### InvalidScheduleDefinitionException

Thrown when a schedule references an undefined event type.

- **Extends:** `RuntimeException`
- **Thrown from:** `MachineDefinition`, `MachineScheduler`
- **See:** [Scheduled Events](/advanced/scheduled-events)

### InvalidListenerDefinitionException

Thrown when listener config uses removed class-as-key format.

- **Extends:** `LogicException`
- **Thrown from:** `StateConfigValidator`
- **See:** [Defining States — Listeners](/building/defining-states#listeners)

### InvalidOutputDefinitionException

Thrown when output is defined on a transient or parallel region state.

- **Extends:** `LogicException`
- **Thrown from:** `StateDefinition`
- **See:** [Outputs](/behaviors/outputs)

### InvalidBehaviorDefinitionException

Thrown for malformed behavior tuples: empty, missing class, closure in tuple.

- **Extends:** `LogicException`
- **Thrown from:** `BehaviorTupleParser`

### MissingBehaviorParameterException

Thrown when a required named parameter is not provided in a behavior tuple.

- **Extends:** `LogicException`
- **Thrown from:** `InvokableBehavior`
- **See:** [Named Parameters](/behaviors/introduction#named-parameters)

### InvalidMachineClassException

Thrown when a job references a machine class that doesn't exist or
doesn't extend `Machine`.

- **Extends:** `LogicException`
- **Thrown from:** `ChildMachineJob`, `SendToMachineJob`
- **See:** [Machine Delegation](/advanced/machine-delegation)

### InvalidJobClassException

Thrown when a job actor class doesn't exist or lacks a `handle()` method.

- **Extends:** `LogicException`
- **Thrown from:** `ChildJobJob`
- **See:** [Job Actors](/advanced/job-actors)

### InvalidTimerDefinitionException

Thrown when a timer duration is zero or negative.

- **Extends:** `LogicException`
- **Thrown from:** `Timer`
- **See:** [Time-Based Events](/advanced/time-based-events)

### MachineDefinitionNotFoundException

Thrown when `definition()` is not implemented on a Machine subclass.

- **Extends:** `RuntimeException`
- **Thrown from:** `Machine`, `MachineScheduler`

### MachineDiscoveryException

Thrown when no valid search paths are found for auto-discovering Machine classes.

- **Extends:** `RuntimeException`
- **Thrown from:** `MachineConfigValidatorCommand`

## Runtime Exceptions

Thrown during machine execution. These indicate conditions that can
legitimately occur at runtime.

### NoTransitionDefinitionFoundException

Thrown when an event has no matching transition in the current state.

- **Extends:** `LogicException`
- **Thrown from:** `MachineDefinition`
- **Caught by:** `SendToMachineJob` (logs warning), `TestMachine` (assertion helpers)
- **See:** [Events](/understanding/events#invalid-events), [Execution Model](/reference/execution-model)

### UndefinedTargetStateException

Thrown when a transition references a target state that doesn't exist
in the machine definition.

- **Extends:** `LogicException`
- **Thrown from:** `TransitionBranch`
- **Previously:** `NoStateDefinitionFoundException`

### MachineValidationException

Thrown when a `ValidationGuardBehavior` fails. Carries Laravel validation
errors. Automatically converted to 422 by endpoint controller.

- **Extends:** `ValidationException`
- **Thrown from:** `Machine`
- **Caught by:** `MachineController` (→ 422), `TestMachine` (assertion helpers)
- **See:** [Validation Guards](/behaviors/validation-guards)

### MachineEventValidationException

Thrown when event payload fails validation defined in `EventBehavior::rules()`.

- **Extends:** `ValidationException`
- **Thrown from:** `EventBehavior`
- **See:** [Events](/behaviors/events)

### MachineContextValidationException

Thrown when machine context fails validation after action execution.

- **Extends:** `ValidationException`
- **Thrown from:** `ContextManager`
- **See:** [Working with Context](/building/working-with-context)

### MaxTransitionDepthExceededException

Thrown when recursive transitions (via `@always` or raised events) exceed
the configured depth limit (default: 100).

- **Extends:** `LogicException`
- **Thrown from:** `MachineDefinition`
- **See:** [Always Transitions](/advanced/always-transitions#infinite-loop-protection)

### MachineAlreadyRunningException

Thrown when a second event is sent to a machine that's already processing.

- **Extends:** `RuntimeException`
- **Thrown from:** `Machine`
- **Caught by:** `SendToMachineJob` (releases back to queue)
- **See:** [Execution Model](/reference/execution-model)

### MachineLockTimeoutException

Thrown when lock acquisition times out during parallel dispatch.

- **Extends:** `RuntimeException`
- **Thrown from:** `MachineLockManager`
- **Caught by:** `Machine` (→ `MachineAlreadyRunningException`), `ListenerJob` (releases)

### NoParentMachineException

Thrown when `sendToParent()` or `dispatchToParent()` is called on a
machine that was not invoked by a parent.

- **Extends:** `RuntimeException`
- **Thrown from:** `InvokableBehavior`
- **See:** [sendTo / sendToParent](/advanced/sendto)

### RestoringStateException

Thrown when machine state cannot be restored from persisted events.

- **Extends:** `RuntimeException`
- **Thrown from:** `Machine`
- **Caught by:** `SendToMachineJob` (logs warning, discards event)

### MissingMachineContextException

Thrown when a behavior accesses a context key that doesn't exist.

- **Extends:** `RuntimeException`
- **Thrown from:** `InvokableBehavior`

### BehaviorNotFoundException

Thrown when a behavior reference cannot be resolved (typo in inline key,
invalid behavior type).

- **Extends:** `RuntimeException`
- **Thrown from:** `ResolvesBehaviors`, `MachineController`

### ArchiveException

Thrown during event archival/restoration: empty collection, compression
failure, decompression failure, invalid data format.

- **Extends:** `RuntimeException`
- **Thrown from:** `MachineEventArchive`, `CompressionManager`
- **See:** [Event Archival](/laravel-integration/archival)

## Testing Exceptions

### BehaviorNotFakedException

Thrown when asserting on a behavior that was never faked via `fake()` or `spy()`.

- **Extends:** `RuntimeException`
- **Thrown from:** `Fakeable` trait
- **See:** [Fakeable Behaviors](/testing/fakeable-behaviors)
```

**Sidebar eklemesi** (`docs/.vitepress/config.ts`, reference section'a):

```ts
{ text: 'Exceptions Reference', link: '/reference/exceptions' },
```

---

## Summary

| Change Type | Count | Details |
|---|---|---|
| New exception classes | 9 | `InvalidStateConfigException`, `InvalidRouterConfigException`, `NoParentMachineException`, `ArchiveException`, `InvalidMachineClassException`, `InvalidJobClassException`, `BehaviorNotFakedException`, `MachineDiscoveryException`, `InvalidTimerDefinitionException` |
| Extended classes | 2 | `InvalidEndpointDefinitionException` (+3 methods), `MachineDefinitionNotFoundException` (+1 method) |
| Existing exceptions wired in | 2 | `BehaviorNotFoundException` → MachineController, `InvalidScheduleDefinitionException` → MachineScheduler |
| Renamed classes | 1 | `NoStateDefinitionFoundException` → `UndefinedTargetStateException` |
| Deleted classes | 1 | `InvalidFinalStateDefinitionException` (merged into `InvalidStateConfigException`) |
| Bug fixes | 1 | `MachineAlreadyRunningException` typo |
| Unchanged (testing infra) | 3 groups | Groups L, M, N — `src/Testing/` exceptions + their test assertions |
| Docs: upgrading guide | 1 file | New "Exception Specialization" section + migration checklist items in `upgrading.md` |
| Docs: in-context | ~15 feature pages | Yeni + genisletilen (11) + henuz dokumante edilmemis mevcut (9) |
| Docs: central reference | 1 new file | `docs/reference/exceptions.md` — 28 exception, 3 kategori |

## Implementation Order

Groups 1–13: exception class creation + src/tests/docs migration.
Groups 14–16: documentation enrichment (upgrading guide, in-context, central reference).

1. **Group A** — `InvalidStateConfigException` (42 src, ~60 tests, 1 doc)
2. **Rename** — `UndefinedTargetStateException` (6 src, 1 test)
3. **Group B** — `InvalidRouterConfigException` (8 src, ~20 tests, 1 doc)
4. **Group C** — Extend `InvalidEndpointDefinitionException` (3 src, ~12 tests)
5. **Group D** — `NoParentMachineException` (2 src, 2 tests, 1 doc)
6. **Group E** — `ArchiveException` (6 src, 4 tests)
7. **Group F** — `InvalidMachineClassException` + `InvalidJobClassException` (4 src, 4 tests)
8. **Group G** — Use `BehaviorNotFoundException` (1 src)
9. **Group H** — Use `InvalidScheduleDefinitionException` + extend `MachineDefinitionNotFoundException` (2 src, 2 tests)
10. **Group I** — `BehaviorNotFakedException` (4 src, 4 tests, 1 doc)
11. **Group J** — `MachineDiscoveryException` (1 src)
12. **Group K** — `InvalidTimerDefinitionException` (1 src, 6 tests, 1 doc)
13. **Bug fix** — `MachineAlreadyRunningException` typo
14. **Docs: upgrading guide** — `docs/getting-started/upgrading.md` "Exception Specialization" section + migration checklist items
15. **Docs: in-context** — Feature docs eksikliklerini gider (10 sayfa) + in-context exception docs (yeni + henuz dokumante edilmemis)
16. **Docs: central reference** — `docs/reference/exceptions.md` + sidebar entry

After all groups: `composer quality` + `vendor/bin/doctest`.
